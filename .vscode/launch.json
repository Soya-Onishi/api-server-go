{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Test Current Dir File",
            "type": "go",
            "request": "launch",
            "mode": "test",
            "program": "${fileDirname}",
            "args": [
                "-test.v"
            ]
        }
    ]
}
